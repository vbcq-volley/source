<!-- title -->


<div class="row page">

  <div class="title">
    <h1>Championnat Groupe <%=page.group%>
    </h1>
  </div>

  <div class="logo droite">
    <img src="local/cache-vignettes/L100xH50/rubon58-06d6b.png?1732290159" class="spip_logo" width="100" height="50"
      alt="" />
  </div>


  Le responsable du Groupe <%=page.group%> est
    <strong><a href="" class="spip_in">Mickaël Poussier</a></strong>

    Pour faciliter la gestion du classement, les résultats sont à
    communiquer au responsable de groupe dès la fin des matchs [*par
    les deux équipes*]

    Pour + d’info consulter l’article suivant&nbsp;:
    <a href="" class="spip_in">Calcul des points du championnat</a>
    <div class="mypage">
      <ul>
        <% site.posts.sort('date', -1).forEach(function(it){ %>
          <% if (it.concern_group && it.concern_group==page.group){ %>
            <li class="listing-item"><a href="<%= url_for(it.path) %>" <% if (it.description) { %> title="<%=
                  it.description %>" <% } %>><%= it.title %></a>
              
            </li>
            <% } %>
              <% }); %>
      </ul>
    </div>

    <table>
      <tr>
        <th>team1</th>
        <th>team2</th>
        <th>homeDate</th>
        <th>awayDate</th>
        <th>homeLocation</th>
        <th>awayLocation</th>
      </tr>
      <%site.data.db.match.entries.filter((it)=>{return it.group==page.group}).forEach(function(it){%>
        <tr>
          <td>
            <%= it.team1 %>
          </td>
          <td>
            <%= it.team2 %>
          </td>
          <td>
            <%= it.homeDate %>
          </td>
          <td>
            <%= it.awayDate %>
          </td>
          <td>
            <%= it.homeLocation %>
          </td>
          <td>
            <%= it.awayLocation %>
          </td>
        </tr>
    

    <% }); %>
</table>

      <!-- Fin rubrique -->


      <%=page.content%>


</div>
<!-- row-fluid -->